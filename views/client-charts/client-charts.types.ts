export interface Attributes {
  id: number;
  client_id: number;
  updated_at: string;
  created_at: string;
  public_name: string;
  description: string;
  abbreviation: string;
  external_name: string;
  max_value: `${number}`;
  min_value: `${number}`;
}
